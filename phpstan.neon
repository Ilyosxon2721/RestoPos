includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 5

    paths:
        - app/

    ignoreErrors:
        - '#Access to an undefined property [A-Za-z\\]+::\$#'
